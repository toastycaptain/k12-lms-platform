# Contracts Package

Baseline API contracts for active services.

- `core-v1.openapi.yaml`: Rails Core `/api/v1` high-traffic endpoints.
- `ai-gateway-v1.openapi.yaml`: FastAPI AI Gateway `/v1` endpoints.

These specs are intentionally scoped as a starting point and should be expanded as endpoints evolve.
