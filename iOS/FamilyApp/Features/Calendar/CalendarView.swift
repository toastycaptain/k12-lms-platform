import SwiftUI

struct CalendarView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Calendar",
            subtitle: "Connected to /api/v1/calendar"
        )
    }
}
