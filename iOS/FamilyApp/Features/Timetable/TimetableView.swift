import SwiftUI

struct TimetableView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Timetable",
            symbol: "clock",
            subtitle: "Connected to family timetable API endpoints"
        )
    }
}
