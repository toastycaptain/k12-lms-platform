import SwiftUI

struct AnnouncementsView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Announcements",
            subtitle: "Connected to /api/v1/announcements"
        )
    }
}
