import SwiftUI

struct ReportsView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Reports",
            subtitle: "Connected to family reports API endpoints"
        )
    }
}
