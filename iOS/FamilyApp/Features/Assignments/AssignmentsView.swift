import SwiftUI

struct AssignmentsView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Assignments",
            symbol: "checklist",
            subtitle: "Connected to family assignment API endpoints"
        )
    }
}
